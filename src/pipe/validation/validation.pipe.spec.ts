import { \src\pipe\validationPipe } from './.\src\pipe\validation.pipe';

describe('\src\pipe\validationPipe', () => {
  it('should be defined', () => {
    expect(new \src\pipe\validationPipe()).toBeDefined();
  });
});
